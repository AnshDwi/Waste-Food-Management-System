{
  "name": "waste-food-management",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/api-gateway",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace api-gateway",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace api-gateway && npm run build --workspace web",
    "test": "npm run test --workspace api-gateway"
  }
}
